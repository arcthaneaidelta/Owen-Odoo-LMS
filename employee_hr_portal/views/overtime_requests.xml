<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="overtime_requests_config_form_view" model="ir.ui.view">
            <field name="name">hr.overtime.requests.form.view</field>
            <field name="model">hr.overtime.requests</field>
            <field name="arch" type="xml">
                <form>
                    <header>
                        <button name="action_reset" invisible="state not in ('approve', 'cancel')" string="Reset To New" type="object" class="oe_highlight"/>
                        <button name="action_approve" invisible="state != 'new'" string="Approve" type="object" class="oe_highlight"/>
                        <button name="update_attendance" invisible="state != 'approve'" string="Update Attendance" type="object" class="oe_highlight"/>
                        <button name="action_refuse" invisible="state != 'new'" string="Refuse" type="object" class="oe_highlight"/>
                        <button name="action_cancel" invisible="state not in ('new', 'approve', 'refuse')" string="Cancel" type="object" class="oe_highlight"/>
                        <field name="state" widget="statusbar" string="confirm" statusbar_visible="new,approve,refuse,cancel" />
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <div class="oe_title">
                                <h1>
                                    <field name="name" required="1" placeholder="XYX..." />
                                </h1>
                            </div>
                        </div>
                        <group>
                            <group>
                                <field name="employee_id" required="1" 
                                    options='{"no_create": True,"no_open": True, "no_create_edit": True}' 
                                    readonly="state != 'new'">
                                </field>
                                <field name="requested_date" readonly="state != 'new'" required="1"></field>
                            </group>
                            <group>
                                <field name="company_id" readonly="1" options='{"no_create": True,"no_open": True, "no_create_edit": True}'></field>
                                <field name="user_id" readonly="1" options='{"no_create": True,"no_open": True, "no_create_edit": True}'></field>
                            </group>
                        </group>
                        <notebook>
                            <page string="Reason/Description" name="reason">
                                <field name="reason" required="1" readonly="state != 'new'"></field>
                            </page>
                        </notebook>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers" />
                        <field name="message_ids" widget="mail_thread" />
                    </div>
                </form>
            </field>
        </record>
        <record id="overtime_requests_config_tree_view" model="ir.ui.view">
            <field name="name">hr.overtime.requests.view.tree</field>
            <field name="model">hr.overtime.requests</field>
            <field name="arch" type="xml">
                <list>
                    <field name="name"></field>
                    <field name="employee_id"></field>
                    <field name="requested_date"></field>
                    <field name="state"></field>
                    <field name="company_id" optional="hide"></field>
                    <field name="user_id" optional="hide"></field>
                </list>
            </field>
        </record>
        <record id="overtime_requests_search_view" model="ir.ui.view">
            <field name="name">hr.overtime.requests.view.search</field>
            <field name="model">hr.overtime.requests</field>
            <field name="arch" type="xml">
                <search string="Overtime Requests">
                    <field name="name"></field>
                    <field name="employee_id"></field>
                    <field name="requested_date"></field>
                    <field name="state"></field>
                    <field name="company_id"></field>
                    <field name="user_id"></field>
                    <separator />
                    <filter name="new" string="New" domain="[('state', '=', 'new')]" />
                    <filter name="approve" string="Approved" domain="[('state', '=', 'approve')]" />
                    <filter name="refuse" string="Refuse" domain="[('state', '=', 'refuse')]" />
                    <filter name="cancel" string="Cancelled" domain="[('state', '=', 'cancel')]" />
                    <separator />
                    <group expand="0" string="Group By">
                        <filter string="Status" name="status" domain="[]" context="{'group_by': 'state'}" />
                        <filter string="Requested Date" name="requested_date" domain="[]" context="{'group_by': 'requested_date'}" />
                        <filter string="Employee" name="employee_id" domain="[]" context="{'group_by': 'employee_id'}" />
                    </group>
                </search>
            </field>
        </record>
        <record model="ir.actions.act_window" id="action_overtime_requests_config">
            <field name="name">Overtime Requests</field>
            <field name="res_model">hr.overtime.requests</field>
            <field name="view_mode">list,form</field>
        </record>
        <menuitem id="menu_overtime_requests_config" name="Overtime Requests" parent="employee_hr_portal.menu_payroll_others" sequence="1" action="action_overtime_requests_config" groups="om_hr_payroll.group_hr_payroll_manager" />
    </data>
</odoo>